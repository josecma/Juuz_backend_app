<html></html>
  <head>
    <style>
      body { 
        font-family: Arial, sans-serif; 
        line-height: 1.6; 
        color: #333; 
        max-width: 600px; 
        margin: 0 auto; 
        padding: 20px; 
      }
      .header { 
        color: #25D606; 
        border-bottom: 2px solid #25D606; 
        padding-bottom: 10px; 
      }
      .button { 
        display: inline-block; 
        background-color: #25D606; 
        color: white; 
        padding: 12px 24px; 
        text-decoration: none; 
        border-radius: 5px; 
        margin: 15px 0;
        font-weight: bold;
        transition: background-color 0.3s;
      }
      .button:hover {
        background-color: #20C005; 
      }
      .footer { 
        margin-top: 20px; 
        font-size: 12px; 
        color: #7f8c8d; 
        border-top: 1px solid #e8f5e9; 
        padding-top: 10px;
      }
      .details {
        background-color: #f0f9ee; 
        padding: 15px;
        border-radius: 5px;
        margin: 15px 0;
        border-left: 4px solid #374957; 
      }
      .verification-code {
        font-size: 28px;
        letter-spacing: 5px;
        color: #374957; 
        font-weight: bold;
        margin: 20px 0;
        text-align: center;
        background-color: #f0f9ee;
        padding: 15px;
        border-radius: 5px;
        border: 1px dashed #25D606;
      }
      a {
        color: #25D606;
      }
      .note {
        font-size: 14px;
        color: #7f8c8d;
        font-style: italic;
      }
      .identity-type {
        font-weight: bold;
        color: #374957;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>Identity Verification for {{platformName}}</h1>
    </div>

    <p>Hello <strong>{{userName}},</strong></p>

    <p>You're verifying your identity as: <span class="identity-type">{{identity}}</span></p>
    
    <p>Please use the following verification code:</p>
    
    <div class="verification-code">
      {{verificationCode}}
    </div>
    
    <div class="details">
      <p><strong>Verification details:</strong></p>
      <ul>
        <li>Requested at: <strong>{{requestTime}}</strong></li>
        <li>Expires in: <strong>{{expirationTime}} minutes</strong></li>
      </ul>
    </div>

    <p class="note">This code confirms your identity. Never share it with anyone, including {{platformName}} staff.</p>
    
    <p>If you didn't request this, please secure your account immediately.</p>
    
    <div class="footer">
      <p>Need help? Contact <a href="mailto:identity@{{platformDomain}}">identity@{{platformDomain}}</a></p>
      <p>© {{currentYear}} {{platformName}}. All rights reserved.</p>
      <p style="font-size: 11px; color: #bdc3c7;">Automated message - please do not reply.</p>
    </div>
  </body>
</html>