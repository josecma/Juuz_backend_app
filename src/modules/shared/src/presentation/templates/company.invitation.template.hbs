<html>
  <head>
    <style>
      body { 
        font-family: Arial, sans-serif; 
        line-height: 1.6; 
        color: #333; 
        max-width: 600px; 
        margin: 0 auto; 
        padding: 20px; 
      }
      .header { 
        color: #25D606; 
        border-bottom: 2px solid #25D606; 
        padding-bottom: 10px; 
      }
      .button { 
        display: inline-block; 
        background-color: #25D606; 
        color: white; 
        padding: 12px 24px; 
        text-decoration: none; 
        border-radius: 5px; 
        margin: 15px 0;
        font-weight: bold;
        transition: background-color 0.3s;
      }
      .button:hover {
        background-color: #20C005; 
      }
      .footer { 
        margin-top: 20px; 
        font-size: 12px; 
        color: #7f8c8d; 
        border-top: 1px solid #e8f5e9; 
        padding-top: 10px;
      }
      .details {
        background-color: #f0f9ee; 
        padding: 15px;
        border-radius: 5px;
        margin: 15px 0;
        border-left: 4px solid #374957; 
      }
      .totp-code {
        font-size: 24px;
        letter-spacing: 3px;
        color: #374957; 
        font-weight: bold;
        margin: 20px 0;
        text-align: center;
        background-color: #f0f9ee; 
        padding: 10px;
        border-radius: 5px;
      }
      a {
        color: #25D606;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>¡You've Been Invited to Join {{platformName}}!</h1>
    </div>

    <p>Hello <strong>{{inviteeName}}</strong>,</p>

    <p>
      <strong>{{inviterName}}</strong> ({{inviterEmail}}) has invited you to join <strong>{{companyName}}</strong> on {{platformName}} as a <strong>{{role}}</strong>.
    </p>

    <div class="details">
      <p><strong>Invitation details:</strong></p>
      <ul>
        <li>Company: <strong>{{companyName}}</strong></li>
        <li>Role: <strong>{{role}}</strong></li>
      </ul>
    </div>

    <p>Click the link below to complete your registration:</p>
    
    <p><a href="{{platformLink}}?email={{inviteeEmail}}" class="button">Accept Invitation</a></p>

    <div class="totp-code">
      your key: {{key}}
    </div>
    
    <div class="footer">
      <p>If you didn't request this invitation or don't recognize {{platformName}}, please ignore this email or contact us at <a href="mailto:support@{{platformDomain}}">support@{{platformDomain}}</a>.</p>
      <p>© {{currentYear}} {{platformName}}. All rights reserved.</p>
      <p style="font-size: 11px; color: #bdc3c7;">This is an automated message, please do not reply directly to this email.</p>
    </div>
  </body>
</html>