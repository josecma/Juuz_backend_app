import { Injectable, Logger } from "@nestjs/common";

@Injectable({})
export default class FindCompanyMemberRoleByIdAdapter {

    private readonly logger = new Logger(FindCompanyMemberRoleByIdAdapter.name);

    public constructor(

    ) { };

};