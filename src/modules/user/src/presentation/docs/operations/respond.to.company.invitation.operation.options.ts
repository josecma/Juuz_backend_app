import { ApiOperationOptions } from "@nestjs/swagger";

export default {
    summary: "user to send a respponse to the invitation",
    description: "the invitation response by user can be accepted or rejected."
} as ApiOperationOptions;