import { ApiOperationOptions } from "@nestjs/swagger";

export default {
    summary: "user sets the notification token",
    description: "user sets the notification token, if token exists it will be updated"
} as ApiOperationOptions;