<html>
  <head>
    <style>
      body { 
        font-family: Arial, sans-serif; 
        line-height: 1.6; 
        color: #333; 
        max-width: 600px; 
        margin: 0 auto; 
        padding: 20px; 
      }
      .header { 
        color: #25D606; 
        border-bottom: 2px solid #25D606; 
        padding-bottom: 10px; 
      }
      .button { 
        display: inline-block; 
        background-color: #25D606; 
        color: white; 
        padding: 12px 24px; 
        text-decoration: none; 
        border-radius: 5px; 
        margin: 15px 0;
        font-weight: bold;
        transition: background-color 0.3s;
      }
      .button:hover {
        background-color: #20C005; 
      }
      .footer { 
        margin-top: 20px; 
        font-size: 12px; 
        color: #7f8c8d; 
        border-top: 1px solid #e8f5e9; 
        padding-top: 10px;
      }
      .details {
        background-color: #f0f9ee; 
        padding: 15px;
        border-radius: 5px;
        margin: 15px 0;
        border-left: 4px solid #374957; 
      }
      a {
        color: #25D606;
      }
      .instructions {
        background-color: #f8f8f8;
        padding: 15px;
        border-radius: 5px;
        margin: 15px 0;
      }
      .highlight-box {
        background-color: #f0f9ee;
        border-left: 4px solid #25D606;
        padding: 12px;
        margin: 15px 0;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>You've Been Invited to Join {{platformName}}!</h1>
    </div>

    <p>Hello <strong>{{inviteeName}}</strong>,</p>

    <p>
      <strong>{{inviterName}}</strong> has invited you to join <strong>{{companyName}}</strong> on {{platformName}} as a <strong>{{role}}</strong>.
    </p>

    <div class="details">
      <p><strong>Invitation Details:</strong></p>
      <ul>
        <li>Company: <strong>{{companyName}}</strong></li>
        <li>Role: <strong>{{role}}</strong></li>
        <li>Platform: <strong>{{platformName}}</strong></li>
        <li>Invitation Time: <strong>{{invitationTime}}</strong></li>
      </ul>
    </div>

    <!-- Bloque condicional (usuario existente vs. nuevo) -->
    {{#if userExists}}
      <div class="instructions">
        <p><strong>How to accept:</strong></p>
        <ol>
          <li>Log in to your existing account at <a href="{{platformLink}}">{{platformLink}}</a>.</li>
          <li>Go to <strong>Notifications</strong> or <strong>Invitations</strong> in your dashboard.</li>
          <li>Click <strong>"Accept"</strong> to join {{companyName}}.</li>
        </ol>
      </div>
    {{else}}
      <div 
      {{!-- class="instructions" --}}
      >
        {{!-- <p><strong>How to accept:</strong></p>
        <ol>
          <li>Create an account <a href="{{platformLink}}/signup?inviteToken={{inviteToken}}&email={{inviteeEmail}}">here</a> (use this email).</li>
          <li>After signing up, you'll be redirected to accept the invitation automatically.</li>
          <li>If not, go to <strong>Notifications</strong> to confirm.</li>
        </ol> --}}
      </div>
    {{/if}}

    <!-- Botón principal (texto dinámico) -->
    <div class="highlight-box">
      <p><strong>Quick acceptance:</strong></p>
      <a 
        href="{{platformLink}}/invitations/accept?token={{inviteToken}}&email={{inviteeEmail}}{{#unless userExists}}&newUser=true{{/unless}}" 
        class="button"
      >
        {{#if userExists}}Accept Invitation Now{{else}}Create Account & Accept{{/if}}
      </a>
      <p style="font-size: 13px; margin-top: 8px; color: #555;">
        This link expires on <strong>{{expiryDate}}</strong>.
      </p>
    </div>

    <div class="footer">
      <p>If you didn't expect this invitation, please ignore it or contact <a href="mailto:support@{{platformDomain}}">support@{{platformDomain}}</a>.</p>
      <p>© {{currentYear}} {{platformName}}. All rights reserved.</p>
      <p style="font-size: 11px; color: #bdc3c7;">This is an automated message—please do not reply directly.</p>
    </div>
  </body>
</html>