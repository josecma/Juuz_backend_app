<html></html>
  <head>
    <style>
      body { 
        font-family: Arial, sans-serif; 
        line-height: 1.6; 
        color: #333; 
        max-width: 600px; 
        margin: 0 auto; 
        padding: 20px; 
      }
      .header { 
        color: #25D606; 
        border-bottom: 2px solid #25D606; 
        padding-bottom: 10px; 
      }
      .button { 
        display: inline-block; 
        background-color: #25D606; 
        color: white; 
        padding: 12px 24px; 
        text-decoration: none; 
        border-radius: 5px; 
        margin: 15px 0;
        font-weight: bold;
        transition: background-color 0.3s;
      }
      .button:hover {
        background-color: #20C005; 
      }
      .footer { 
        margin-top: 20px; 
        font-size: 12px; 
        color: #7f8c8d; 
        border-top: 1px solid #e8f5e9; 
        padding-top: 10px;
      }
      .details {
        background-color: #f0f9ee; 
        padding: 15px;
        border-radius: 5px;
        margin: 15px 0;
        border-left: 4px solid #374957; 
      }
      a {
        color: #25D606;
      }
      .instructions {
        background-color: #f8f8f8;
        padding: 15px;
        border-radius: 5px;
        margin: 15px 0;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>You've Been Invited to Join {{platformName}}!</h1>
    </div>

    <p>Hello <strong>{{inviteeName}}</strong></p>

    <p>
      <strong>{{inviterName}}</strong> has invited you to join <strong>{{companyName}}</strong> on {{platformName}} as a <strong>{{role}}</strong>.
    </p>

    <div class="details">
      <p><strong>Invitation Details:</strong></p>
      <ul>
        <li>Company: <strong>{{companyName}}</strong></li>
        <li>Role: <strong>{{role}}</strong></li>
        <li>Platform: <strong>{{platformName}}</strong></li>
      </ul>
    </div>

    <div class="instructions">
      <p><strong>To accept this invitation:</strong></p>
      <ol>
        <li>Log in to your {{platformName}} account (<a href="{{platformLink}}">{{platformLink}}</a>)</li>
        <li>Go to the "Invitations" section in your profile</li>
        <li>Look for the invitation from <strong>{{companyName}}</strong></li>
        <li>Click "Accept Invitation"</li>
      </ol>
      
      <p>Don’t have an account yet? Sign up first using the same email address this invitation was sent to.</p>
    </div>

    <p>You can also accept directly by clicking the button below:</p>
    
    <p><a href="{{platformLink}}/invitations?email={{inviteeEmail}}" class="button">Go to {{platformName}} to Accept</a></p>
    
    <div class="footer">
      <p>If you didn’t request this invitation or don’t recognize {{platformName}}, please ignore this email or contact us at <a href="mailto:support@{{platformDomain}}">support@{{platformDomain}}</a>.</p>
      <p>© {{currentYear}} {{platformName}}. All rights reserved.</p>
      <p style="font-size: 11px; color: #bdc3c7;">This is an automated message—please do not reply directly to this email.</p>
    </div>
  </body>
</html>