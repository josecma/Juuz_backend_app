<html></html>
  <head>
    <style>
      body { 
        font-family: Arial, sans-serif; 
        line-height: 1.6; 
        color: #333; 
        max-width: 600px; 
        margin: 0 auto; 
        padding: 20px; 
      }
      .header { 
        color: #25D606; 
        border-bottom: 2px solid #25D606; 
        padding-bottom: 10px; 
      }
      .button { 
        display: inline-block; 
        background-color: #25D606; 
        color: white; 
        padding: 12px 24px; 
        text-decoration: none; 
        border-radius: 5px; 
        margin: 15px 0;
        font-weight: bold;
        transition: background-color 0.3s;
      }
      .button:hover {
        background-color: #20C005; 
      }
      .footer { 
        margin-top: 20px; 
        font-size: 12px; 
        color: #7f8c8d; 
        border-top: 1px solid #e8f5e9; 
        padding-top: 10px;
      }
      .welcome-box {
        background-color: #f0f9ee;
        padding: 20px;
        border-radius: 8px;
        margin: 20px 0;
        text-align: center;
        border-left: 4px solid #374957;
      }
      .icon {
        font-size: 48px;
        color: #25D606;
        margin-bottom: 15px;
      }
      .credentials {
        background-color: #f8f8f8;
        padding: 15px;
        border-radius: 5px;
        margin: 15px 0;
        font-family: monospace;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1> Your {{platformName}} Account Has Been Created!</h1>
    </div>

    <p>Hello <strong>{{userName}}</strong>,</p>
    
    <div class="welcome-box">
      <div class="icon">✓</div>
      <h3 style="margin-top: 0;">Welcome to {{companyName}}</h3>
      <p>An account has been created for you on our platform.</p>
    </div>

    <p>You are now an official member of <strong>{{companyName}}</strong> on {{platformName}} with the role of <strong>{{role}}</strong>.</p>

    <div class="credentials">
      <p><strong>Your initial credentials:</strong></p>
      <p>Email: <strong>{{userEmail}}</strong></p>
      <p>Access: <a href="{{platformLink}}" style="word-break: break-all;">{{platformLink}}</a></p>
    </div>

    <div style="background-color: #f0f9ee; border-left: 4px solid #25D606; padding: 12px; margin: 20px 0;">
      <p><strong>Getting started:</strong></p>
      <ol>
        <li>Log in for the first time using <a href="{{loginLink}}">this link</a></li>
        {{!-- <li>Set up a secure password</li> --}}
        <li>Complete your profile (optional but recommended)</li>
      </ol>
      <a href="{{loginLink}}" class="button">Log In for the First Time</a>
    </div>

    <p>If you have any questions or need assistance, please contact your company administrator or our support team.</p>

    <div class="footer">
      <p>This is an automated message. If you received this email by mistake, please notify <a href="mailto:support@{{platformDomain}}">support@{{platformDomain}}</a>.</p>
      <p>© {{currentYear}} {{platformName}}. All rights reserved.</p>
    </div>
  </body>
</html>